# CSV Data Path (used in csv mode)
CSV_PATH=../sql.csv

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000

# Discord CTA Link
DISCORD_URL=https://discord.gg/codebasics

# App Mode: "csv" for MVP fallback, "rag" for production
APP_MODE=csv

# MySQL Database (for data ingestion)
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=

# Qdrant Cloud
QDRANT_URL=
QDRANT_API_KEY=

# Embedding Model (default: all-MiniLM-L6-v2)
EMBEDDING_MODEL=all-MiniLM-L6-v2
